# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~



# Home page
GET /									controllers.Website.index()

# request chart website + some resources
GET /charts/$chartName<[a-z]+>.html		controllers.Website.chartType(chartName)
GET /charts/$chartName<[a-z]+>.js		controllers.Website.chartJS(chartName)
GET /charts/$chartName<[a-z]+>/*file	controllers.Website.chartStatics(chartName, file)

# history overview
GET /history							controllers.Website.historyOfCharts()

#admin
GET /adminLogin							controllers.Website.adminLogin()
POST /adminLogin						controllers.Website.login()
GET /admin								controllers.Website.admin()
POST /admin								controllers.Website.logfile()
GET /logout								controllers.Website.logout()

#request a chart visualization in json
POST /requestChart						controllers.Website.requestChart()
GET	 /requestHistory					controllers.Website.requestHistory(hist)

#change language
POST /language							controllers.Website.changeLanguage()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)


# for test purposes, lock class for more infos
GET /TEST								controllers.Test.test()